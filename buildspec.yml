version: 0.2

phases:
    install:
          runtime-versions:
                  java: corretto8
                    pre_build:
                          commands:
                                  - echo Initializing environment
                                        - export CODEARTIFACT_AUTH_TOKEN=`aws codeartifact get-authorization-token --domain heyprashant --domain-owner 992382818387 --query authorizationToken --output text`
                                          build:
                                                commands:
                                                        - echo Build started on `date`
                                                              - mvn -s nextwork-web-project/settings.xml compile
                                                                post_build:
                                                                      commands:
                                                                              - echo Build completed on `date`
                                                                                    - mvn -s nextwork-web-project/settings.xml package
                                                                      artifacts:
                                                                          files:
                                                                                - target/nextwork-web-project.war
                                                                                  discard-paths: no
